apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "krusty.fullname" . }}-test-connection"
  labels:
    {{- include "krusty.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "krusty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
