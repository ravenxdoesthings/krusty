apiVersion: v1
kind: Service
metadata:
  name: {{ include "krusty.fullname" . }}
  labels:
    {{- include "krusty.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krusty.selectorLabels" . | nindent 4 }}
